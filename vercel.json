{
  "version": 2,
  "installCommand": "python -m pip install --upgrade pip setuptools wheel; python -m pip install --no-cache-dir --use-pep517 -r api/requirements.txt",
  "routes": [
    { "src": "/(.*)", "dest": "/api/app.py" }
  ]
}
