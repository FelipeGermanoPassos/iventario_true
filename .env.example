# Configuração do Banco de Dados
# IMPORTANTE PARA VERCEL: Use o PgBouncer pooler (porta 6543), NÃO a porta direta (5432)
# 
# MOTIVO: Vercel tem problemas com IPv6. O pooler geralmente resolve melhor para IPv4.
# Se ainda tiver problemas de conexão, tente:
# 1. Pooler (recomendado para Vercel): porta 6543
# 2. Direto (fallback): porta 5432
#
# Pooler Format (porta 6543):
#   postgresql://postgres.<project-ref>:<database_password>@aws-0-<region>.pooler.supabase.com:6543/postgres?sslmode=require
#
# Direto (porta 5432):
#   postgresql://postgres:<database_password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require
#
# Exemplo prático:
# DATABASE_URL=postgresql://postgres.ttfpqsdctnkzwrrhfssb:100901Fe@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require

DATABASE_URL=

# Chave Secreta (gere uma aleatória para produção)
SECRET_KEY=dev-secret-key-change-in-production

# Configuração de E-mail (Opcional)
MAIL_ENABLED=false
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=seu-email@gmail.com
MAIL_PASSWORD=sua-senha-de-app
MAIL_DEFAULT_SENDER=seu-email@gmail.com

# Notificações Push (Opcional)
# VAPID_PUBLIC_KEY=sua-chave-publica
# VAPID_PRIVATE_KEY=sua-chave-privada

# WhatsApp (Opcional)
# WHATSAPP_ENABLED=false
# WHATSAPP_PROVIDER=twilio

# Telegram (Opcional)
# TELEGRAM_ENABLED=false
# TELEGRAM_BOT_TOKEN=seu-token
