# Configuração de E-mail para o Sistema de Inventário
# Copie este arquivo para .env e preencha com suas credenciais

# ====== CONFIGURAÇÃO DE E-MAIL ======

# Habilitar/Desabilitar envio de e-mails (true ou false)
MAIL_ENABLED=true

# Servidor SMTP
# Gmail: smtp.gmail.com
# Outlook/Hotmail: smtp-mail.outlook.com
# Yahoo: smtp.mail.yahoo.com
# TrueBrands/Locaweb: smtp.truebrands.com.br ou smtp.hostgator.com.br
MAIL_SERVER=smtp.gmail.com

# Porta SMTP
# 587 = TLS (recomendado)
# 465 = SSL
# 25 = Sem criptografia (não recomendado)
MAIL_PORT=465

# Usar TLS (true ou false) - para porta 587
MAIL_USE_TLS=false

# Usar SSL (true ou false) - para porta 465
MAIL_USE_SSL=true

# Seu e-mail (remetente)
MAIL_USERNAME=noreply@truebrands.com.br

# Senha do e-mail
# ATENÇÃO: Se usar e-mail corporativo (@truebrands.com.br), você precisa:
# 1. Descobrir o servidor SMTP da sua empresa (pergunte ao TI)
# 2. Ajustar MAIL_SERVER para o servidor correto
# 3. Verificar se a porta é 587 (TLS) ou 465 (SSL)
#
# IMPORTANTE: Para Gmail, use uma "Senha de App" e não sua senha normal
# Como gerar senha de app no Gmail:
# 1. Acesse https://myaccount.google.com/security
# 2. Ative a verificação em duas etapas
# 3. Vá em "Senhas de app" e gere uma nova senha
# 4. Use essa senha aqui
MAIL_PASSWORD=tepu bdyd rdjx yghe

# E-mail padrão do remetente (opcional, se não definido usa MAIL_USERNAME)
MAIL_DEFAULT_SENDER=noreply@truebrands.com.br

# ====== COMO USAR ======

# Windows PowerShell:
# $env:MAIL_ENABLED="true"
# $env:MAIL_SERVER="smtp.gmail.com"
# $env:MAIL_PORT="587"
# $env:MAIL_USE_TLS="true"
# $env:MAIL_USERNAME="seu-email@gmail.com"
# $env:MAIL_PASSWORD="sua-senha-app"
# python run.py

# Linux/Mac:
# export MAIL_ENABLED=true
# export MAIL_SERVER=smtp.gmail.com
# export MAIL_PORT=587
# export MAIL_USE_TLS=true
# export MAIL_USERNAME=seu-email@gmail.com
# export MAIL_PASSWORD=sua-senha-app
# python run.py

# ====== TESTANDO ======
# Após configurar, execute o sistema e:
# 1. Registre um empréstimo com e-mail válido
# 2. Verifique se recebeu o e-mail de confirmação
# 3. Os lembretes serão enviados diariamente às 09:00
# 4. Configure a data de devolução para próxima para testar lembretes
